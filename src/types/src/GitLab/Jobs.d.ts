import AbstractList = require("./AbstractList");
import Responses = require("./Responses");

export = Jobs;

declare class Jobs extends Responses {
    /**
     * @param {[]|AbstractList} v
     */
    constructor(v: [] | AbstractList);
}


//# sourceMappingURL=Jobs.d.ts.map